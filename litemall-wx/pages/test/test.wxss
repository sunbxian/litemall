/* 基础样式 */
page {
  height: 100%;
  background-color: #f7f7f7;
}

.container {
  position: relative;
  height: 100%;
  width: 100%;
}

.main-scroll {
  width: 100%;
}

/* Banner区域样式 */
.banner {
  width: 100%;
  overflow: hidden;
}

.banner-text {
  text-align: center;
  margin-top: 10px;
  color: #333;
  font-size: 14px;
  background-color: #fff; /* 添加背景色以确保文字清晰 */
  padding: 5px 0;
}

/* 内容区域样式 */
.content {
  display: flex;
  min-height: calc(100vh - 200px); /* 减去banner高度 */
  flex-wrap: nowrap; /* 确保子元素不换行 */
}

/* 分类列表样式 */
.category-list {
  width: 25%;
  background: #fff;
  box-sizing: border-box;
}

.category-list.fixed {
  position: fixed;
  top: 0;
  left: 0;
  opacity: 0; /* 原始元素隐藏，使用克隆元素 */
  pointer-events: none;
}

.fixed-clone {
  position: fixed;
  left: 0;
  width: 25%;
  background: #fff;
  z-index: 20;
  box-sizing: border-box;
}

.category-item {
  padding: 15px 10px;
  text-align: center;
  font-size: 14px;
  border-left: 3px solid transparent;
  transition: all 0.2s;
} 

.category-item.active {
  background: #f5f5f5;  
  border-left: 3px solid #FF4D4F;
  color: #FF4D4F;
  font-weight: bold;
}

/* 商品列表样式 */
.product-list {
  width: 75%;
  box-sizing: border-box;
  padding: 10px;
  flex-shrink: 0; /* 防止宽度被压缩 */
  background: #f5f5f5; 
}

/* .product-item {
  display: flex;
  margin-bottom: 15px;
  background: #fff;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
} */

/* .product-image {
  width: 100px;
  height: 100px;
  flex-shrink: 0;
} */

/* .product-info {
  flex: 1;
  padding: 10px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.product-name {
  font-size: 14px;
  color: #333;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  line-height: 1.4;
}

.product-price {
  font-size: 16px;
  color: #FF4D4F;
  font-weight: bold;
}

.product-sales {
  font-size: 12px;
  color: #999;
} */
  
  
 

.product-list .item {
  display: flex; 
  background: #fff;
  margin-bottom: 20rpx;
  padding: 20rpx;
  border-radius: 10rpx;
 
}

.product-list .item .img {
  width: 200rpx;
  height: 200rpx;
  margin-right: 20rpx;
  flex-shrink: 0; /* 确保图标位置固定 */
  border-radius: 8rpx;
} 

.product-list .item .info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.product-list .item .info .name {
  font-size: 32rpx; /* 字体加大 */ 
  color: #000; /* 黑色 */
  margin-bottom: 10rpx;
  white-space: normal; /* 允许换行 */
  word-wrap: break-word; /* 长单词换行 */
  word-break: break-word; /* 强制长单词换行 */
  line-height: 40rpx; /* 增加行高以便更好显示多行文本 */
}

.product-list .item .info .desc {
  font-size: 24rpx;
  color: #666;
  white-space: normal; /* 允许换行 */
  word-wrap: break-word; /* 长单词换行 */
  word-break: break-word; /* 强制长单词换行 */
  line-height: 40rpx; /* 增加行高以便更好显示多行文本 */
  margin-bottom: 10rpx;
}

.product-list .item .info .price {
  display: flex;
  align-items: baseline; /* 对齐基线 */
}

.product-list .item .info .price .currency {
  font-size: 24rpx; /* ￥ 的字体大小 */
  color:  #FF4D4F;
  margin-right: 5rpx; /* 与价格的间距 */
}

.product-list .item .info .price .amount {
  font-size: 32rpx; /* 价格的字体大小 */
  color:   #FF4D4F;
  font-weight: bold; /* 加粗 */
}

.product-list .item .info .original-price {
  font-size: 24rpx; /* 原价字体大小 */
  color: #999; /* 灰色 */
  text-decoration: line-through; /* 删除线 */
  margin-bottom: 10rpx;
} 

.loading-more {
  text-align: center;
  padding: 10px;
  color: #999;
  font-size: 12px;
}